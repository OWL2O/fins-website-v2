// ChatWidget.jsx — replaced by the premium FINS AI assistant.
// The old static support widget (AnyDesk / phone / WhatsApp) has been
// superseded. This file is intentionally a thin re-export so the
// Home.jsx import doesn't need to change.

export { default } from './AiChat.jsx';
